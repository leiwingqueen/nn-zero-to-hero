# 课程环境依赖（Python 3.12）
# 安装：uv venv --python 3.12 .venv && VIRTUAL_ENV=.venv uv pip install -r requirements.txt
# 或：  python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
torch==2.14.0
matplotlib==3.11.1
jupyter==1.1.1
ipykernel==7.3.0
